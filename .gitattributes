# Normalize line endings: store text files as LF in the repo.
# This prevents Windows CRLF from showing every file as "modified".
* text=auto eol=lf

# Binary assets — never modify these.
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
